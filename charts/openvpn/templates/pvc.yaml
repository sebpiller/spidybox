---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: "{{ .Release.Namespace }}"
  name: openvpn-config-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeName: spidybox-volume
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: "{{ .Release.Namespace }}"
  name: openvpn-downloads-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeName: spidybox-volume
  resources:
    requests:
      storage: 10Gi
