---
apiVersion: v1
kind: Service
metadata:
  namespace: "{{ .Release.Namespace }}"
  name: radarr-svc
spec:
  type: NodePort
  ports:
    - port: 7878
      targetPort: 7878
      nodePort: 30078
  selector:
    app: radarr