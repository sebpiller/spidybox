---
apiVersion: v1
kind: Service
metadata:
  namespace: "{{ .Release.Namespace }}"
  name: sonarr
spec:
  type: NodePort
  ports:
    - port: 8989
      targetPort: 8989
      nodePort: 30089
  selector:
    app: sonarr